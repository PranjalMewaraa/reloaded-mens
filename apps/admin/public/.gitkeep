# Sprint 9 deploy — Next.js standalone build references apps/<app>/public
# (Dockerfile COPY step). Even if the admin has no static assets, the
# directory must exist so the COPY doesn't fail. Real static files (favicon,
# admin avatar fallback) land here when added.
